这一个程序应该用到结构体，文件操作，链表的知识，文件的读和写。

应该用到指针，高效的管理内存。

要保证动态管理内存，用mallloc函数和其它的。

我需要保证代码的简洁性和高效性，少定义变量，多多利用全局变量，能不写多余的花括号就不写，要用最适合的语法知识。

还应该向用户询问年月日，作为生成的文件名字。

还得保证代码的可读性，多用一些高级的知识，并思考这些知识的意义。

如果一切顺利，那么下个礼拜星期六应该能写完。

理想的代码效果是这样的，程序问用户输入几个人的身体数据（用链表，动态内存管理）,然后用户输入一个数，比如50,也许应该用文件读fscanf()函数，先打开一个文件，让用户写入数据，之后程序开始处理，并生成另一个文件(用fprintf)写入。

这样，就不用用户一个一个数据的手输了，这样太没有效率了，还不优雅。

也许要先画一画流程图。

我必须得熟练的掌握fprintf,和fscanf才行。
