#include <stdio.h>
#include <math.h>
int main(void)
{
     float a,b,c;
     printf("shu ru a b c:");
     scanf("%f %f %f",&a,&b,&c);
     if(a !=o )
    {
        if(b*b-4*a*c>0)
        {
            b*b-4*a*c=beita;
            x1=(-b+sqrt(beita))/2a;
            x2=(-b-sqrt(beita))/2a;
            printf("%f %f",x1,x2);
        }
        els)
 

    }
     else
         printf("you no san jiao xing!!");


}
