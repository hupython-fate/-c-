#include <stdio.h>
int main(void)
{
	int a,b;
	for(a=0,b=0;a==b;a++,b=b+a)//嗯？为什么输出没有结果？因为==才是比较运算符！
	printf("%4d %4d\n",a,b);
	
	int a=321,654;//为什么会报错？难道不相等？ 
	//上面这一句，等同于：
	int a=321;
	654;
	//两者是等价的。 
	/*报错原因：
这行代码的语法含义是：声明一个整型变量 a 初始化为 321，然后声明另一个整型变量 654（但没有初始化）。

在C语言中：

int a=321,654; 被解析为：int a=321 和 int 654

变量名不能以数字开头，654 不是合法的变量名

因此编译器会报错：expected identifier*/ 
	return 0; 
} 
