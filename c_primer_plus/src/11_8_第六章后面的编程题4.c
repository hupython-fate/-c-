#include <stdio.h>
int main(void)
{
	int a;
	int b,c;
	printf("%d,%d,%d",'A','B','C');//65,66,67/
        printf("\n");
	for(a=65,c=1;a<91;c++)
	{
		for(b=0;b<c;b++,a++)
			printf("%c",a);
		printf("\n");
	}
    return 0;
}
//第一次循环,a=65,c=1,65<91成立.b=0,0<1成立,打印65对应的字符,之后a++,b++,打印完换行符后,c++.
//第二次循环,a=66,c=2,66<91成立,b=0,0<2成立,打应66对应的字符,之后b++,a++,b=1,1<2成立,打印67对应的字符,之后又执行b++,a++,所以a=68..
//第三次循环,a=68,c=3,68<91成立,b=0,0<3成立,打68对应的字符,之后b++,a++,b=1,1<3成立,打印69对应的字符~~~
//从逻辑上看,这应该能达到我想要的结果.
