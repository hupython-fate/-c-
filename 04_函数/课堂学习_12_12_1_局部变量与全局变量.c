#include <stdio.h>

int main(void)
{

	//变量在c语言中分为局部变量和全局变量，每个不同类别的变量有不同的储存类型，每个储存类型分别对应了一个关键字，也就时说，有四个关键字要学习
	
	//一，局部变量的储存类别
	//自动局部变量------auto
	//静态局部变量------static
	//
	//二，全局变量的储存类别
	//把一个全局变量的作用域括到其他文件------extern
	//把一个全局变量的作用域限制在本文件内------static
	//
	//由于auto不需要显式使用，而实际上也只有两个关键字要学会用，extern和static。
	return 0;
}
