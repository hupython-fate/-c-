#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    return 0;
}

/*
二叉树，顾名思义，就是结点的度小于等于二的树。

换而言之，就是最多只有两个分叉的树。

为什么要重点研究二叉树呢？

1,二叉树的结构最简单，规律性最强；
2,可以证明，所有的树都可以转成唯一个二叉树，不失一般性。
3,普通的树（多叉树），如果不转化成多插叉树，则运算很难实现。
*/

/*
所以，所谓的学习和应用树，主要就是学习和掌握二叉树？这真是一个令人激动的发现。

二叉树是n个结点的有限集，它或者是空集（n==0），或者由一个根结点及两颗互不交互的分别称作这个根的左子树和右子树的二叉树组成。

一个不太理解的知识：二叉树不是树的特殊情况，它们是两个概念。

*/